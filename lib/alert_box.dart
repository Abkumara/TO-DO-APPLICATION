import 'package:flutter/material.dart';

class ShowAlertBox extends StatelessWidget {
  const ShowAlertBox({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      title: Text('data'),
    );
  }
}
